$(".comment-count").html("<%= @want_post.want_comments.count %> comments!");
$(".post-comment").html("<%= j(render "users/want_comments/index", want_post: @want_post, want_comment: @want_comment) %>");
$("textarea").val('')